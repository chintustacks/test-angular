import { PrimenumberPipesPipe } from './primenumber-pipes.pipe';

describe('PrimenumberPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimenumberPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
