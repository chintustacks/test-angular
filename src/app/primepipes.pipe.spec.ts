import { PrimepipesPipe } from './primepipes.pipe';

describe('PrimepipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimepipesPipe();
    expect(pipe).toBeTruthy();
  });
});
